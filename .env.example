# Copier ce fichier en .env pour lancer l'application localement :
#   cp .env.example .env
#
# NE JAMAIS commiter le fichier .env (voir .gitignore).

DB_NAME=appdb
DB_USER=appuser
DB_PASSWORD=apppassword
